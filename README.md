# v0.16.9b · 首頁 Hero 定位調整器

這是一個暫時的開發調整器，不是正式網站功能。

上傳覆蓋後，首頁右下角會出現「首頁圖定位器」。

可即時調整：
- 左右 X
- 上下 Y
- Hero 高度
- 圖片縮放

數值會直接顯示成 CSS：

--home-hero-x
--home-hero-y
--home-hero-height
--home-hero-scale

按「記住位置」會存到目前瀏覽器 localStorage，重新整理後仍保留。

調到滿意後，把面板上的四個數值告訴我，我再幫你做正式版並移除定位器。
