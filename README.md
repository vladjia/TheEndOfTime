# 《時盡》網站核心 v0.16.4

這一版開始把「網站核心」與「內容資料」分離，方便未來持續改版。

## 結構

- `index.html`：公開入口 + SEO / Open Graph
- `app/site.css`：網站視覺與動畫
- `app/site.js`：資料讀取與互動
- `data/config.json`：版本、Google Drive/GAS endpoint、字型設定
- `data/characters.json`：公開角色資料
- `data/world.json`：公開世界觀資料
- `data/story.json`：公開故事文字與未來劇透 endpoint
- `assets/images/og-cover.png`：LINE / Facebook 分享預覽圖
- `assets/fonts/`：正式網站字型位置
- `assets/effects/`：特效素材

## GitHub / Pages

- Repository: https://github.com/vladjia/TheEndOfTime
- GitHub Pages: https://vladjia.github.io/TheEndOfTime/

OG 分享爬蟲通常不執行 JavaScript，所以 `og:url` 與 `og:image` 必須直接寫在 index.html 裡，不能只放 config.json。

## LINE / Facebook 預覽

目前已包含：
- meta description
- og:title
- og:description
- og:type
- og:url
- og:image
- og:image:width / height
- twitter:card

預覽圖尺寸：1200 × 630。

## 未來更新方式

版型/功能更新通常只覆蓋：
- `app/site.css`
- `app/site.js`
- 必要時 `index.html`

角色內容與圖片來源會逐步移到資料層 / Google Drive + GAS，避免改版時重做。


## v0.5：Google Sheet / GAS 已接線

公開 API：
https://script.google.com/macros/s/AKfycbwpF5cAHbnIFphMdrd5jLcGs0rmt8li7aYSZ4Y47CU1tEOLwYXMv1bVDKOqQqKilOA/exec

網站載入順序：
1. 先讀 `data/config.json` 取得 GAS endpoint
2. 呼叫 GAS `?type=public`
3. 成功時以 Google Sheet / Drive 資料渲染
4. GAS 暫時失效時，自動退回 GitHub 內的本機 JSON 備援資料

### 重要安全規則
公開 Web App 不應輸出 `authorNotes`。
作者模式日後會改成獨立、需要授權的 GAS / Google 登入流程。


## v0.6：特殊展示字型

新增：
- `assets/fonts/TheEndOfTimeDisplay.ttf`
- CSS `@font-face`
- `--font-display`

特殊展示字型只套用：
- 首頁「時盡」
- 章節大標
- 角色正式稱號
- 世界觀標題
- 關鍵引言
- 品牌「時」字

正文仍使用清楚的系統黑體，避免長時間閱讀造成視覺疲勞。


## v0.7：角色印記 / Living Seals

古風展示字體大幅收斂：
- 保留：首頁「時盡」、左上「時」、角色印記
- 恢復原字體：角色名稱、章節標題、世界觀標題、引言、正文

角色卡右下角識別印記：
- 家式 → 家：停時 / 短暫切片感
- 白霽 → 白：極輕微呼吸漂移
- 葉慎行 → 月半：沉穩，hover 出現刀痕
- 安言修 → 蝶：偶爾出現黑蝶殘影

動畫皆支援 `prefers-reduced-motion`。


## v0.7.1：角色印記位置修正
- 四張角色卡右下角印記整體上提
- 不再讓字被卡片底邊裁切
- 「月半」使用獨立位置，確保雙字完整顯示


## v0.8：首頁進場動畫

首頁正式故事鉤子：
「他活過世界的終點，卻沒能逃過那些早已注定的結局。」

核心法則：
「過程可以改變，結果不能。」

進場節奏：
1. 一道極細時間沙落下
2. 沙在途中停止
3. THE END OF TIME 淡入
4. 「時盡」像從黑暗中顯影
5. 故事鉤子出現
6. 世界法則最後亮起極淡血紅
7. 操作按鈕出現
8. 動畫停止，不循環

主視覺只做暗處顯影，不滑入、不縮放、不讓人物持續晃動。


## v0.9：迎賓頁 / START

首頁前新增獨立迎賓層：
- 全黑冷色背景
- THE END OF TIME
- 古風展示字「時盡」
- START 按鈕
- 一道極細時間沙落下後停住

按下 START 後：
- 迎賓頁淡出
- 才開始播放真正首頁的進場動畫
- 首頁故事鉤子與核心法則保留既有定案
- 不加入 BGM / SFX，避免自動播放與干擾；音訊系統可另案設計

README 首行版本號自本版起同步更新。


## v0.9.1：迎賓頁細修

- 「時盡」兩字加大字距，避免過於擁擠
- `THE END OF TIME` 移到「時盡」下方
- 英文改為低透明、輕微虛化、向下漸隱的時間殘影
- 只加入極輕微折射漂移，不做明顯水波
- START 位置重新平衡
- 手機版另行縮小字距與英文殘影比例


## v0.9.2：迎賓頁轉化式重構

迎賓頁不再把 `THE END OF TIME` 當成小型副標，而改為：

- 上層：大型古風字「時盡」
- 中層：細微流沙 / 時間漏落
- 下層：大型英文概念形體 `THE END OF TIME`
- 最下：START

設計目的：
- 讓畫面讀起來像「時盡」往下漏成 `THE END OF TIME`
- 英文與中文形成上下呼應，而不是標題 / 副標關係
- START 獨立成第三層操作，不再貼近英文殘影

另外保留：
- 全黑迎賓背景
- 極淡血紅氛圍
- 不加入突兀的大量特效
- 無障礙減少動態支援


## v0.9.3：真流沙修正版

- 移除 v0.9.2 中央那條突兀的直線與假顆粒
- 改用 Canvas 真粒子流沙
- 流沙從「時」「盡」兩個字的下方區域分別落下
- 粒子下墜時有重力、輕微左右漂移與逐步消散
- 少量粒子帶極淡血紅，仍以冷白灰為主
- THE END OF TIME 稍微提高可讀性，但仍維持虛化與時間殘影感
- START 維持獨立，不與英文黏在一起


## v0.9.4：流沙可視修正

這版專門修正「看不到流沙」：
- CSS / JS 加入版本參數，避免 GitHub Pages / 瀏覽器沿用舊快取
- Canvas 初始化時先預填粒子，不再先出現一大片空白
- 粒子密度提高
- 下落速度放慢
- 粒徑稍微加大
- 部分粒子加入短拖尾，讓「流」更容易被看見
- 粒子從「時」「盡」兩個字底部寬區域分別落下，而不是中央單點


## v0.9.5：細緻流沙重製

重做 v0.9.4 粗糙的粒子效果：
- 不再使用明顯大白點
- 流沙改從「時」「盡」實際筆畫下緣採樣後脫落
- 95% 粒子改為超細粉砂，只有極少數較大的碎屑
- 取消雪花 / 火星感
- 不使用中央束口、白色光柱或粗拖尾
- 下墜加入極小幅空氣偏移，避免像雨
- `THE END OF TIME` 減少模糊，保留虛化但提升質感
- CSS / JS 版本參數更新為 v0.9.5，避免舊快取


## v0.9.6：流沙對位修正

修正 v0.9.5 流沙整體偏右：
- 不再用 Canvas 重新估算「時」「盡」的位置
- 改為直接讀取畫面上兩個字的實際 DOM 邊界
- 流沙分別從兩字正下方的寬區域落下
- 左右粒子量維持平均
- 粒子再縮細一點，避免重新變回粗糙顆粒


## v0.10.0：「時盡」沙化進場

START 正式移除。

新的迎賓互動：
1. 直接點擊「時盡」
2. 字體從下緣開始被時間侵蝕
3. 筆畫化為細沙下墜
4. `THE END OF TIME` 從模糊殘影逐漸聚合
5. 迎賓頁像被抽走一層時間後淡出
6. 正式首頁開始原有進場動畫

細節：
- hover / focus 時只掉極少量沙，暗示標題可互動
- 點擊後使用三波不同強度的細砂脫落
- 只取筆畫下緣作為粒子來源，不做整個中文字爆炸
- 粒子以細粉砂為主，極少數稍大碎屑
- 少量極淡血紅粒子保留《時盡》配色
- 手機與 reduced-motion 均有相容處理
- CSS / JS cache-bust 更新至 v0.10.0


## v0.10.1：JavaScript 語法修正

修正 v0.10.0 `site.js` 第 136 行的：
`Uncaught SyntaxError: Unexpected token 'else'`

原因：
- 移除舊迎賓函式時，殘留了兩個孤立的 `else` 區塊。

本版：
- 完整移除殘留 `else`
- 清除舊的 `initWelcomeGate()` / `initWelcomeSand()` 呼叫
- 保留 v0.10.0 的「點擊時盡 → 沙化 → 進場」設計
- CSS / JS cache-bust 更新為 v0.10.1


## v0.10.2：沙化重構修正

這版不是再調參數，而是重做座標與消散方式：

- 移除 CSS mask，因此不再一格一格消失
- 「時盡」改用連續透明度 + blur 平滑退去
- 新增 `welcome-title-stage`，讓「時盡」與粒子 Canvas 共用同一座標系
- 粒子直接讀取畫面上「時」「盡」兩個 DOM 字框的實際位置
- 侵蝕線從字底向上推，沙粒跟著實際字框產生
- hover 只掉極少量細砂；點擊後才正式沙化
- 流沙錯位問題與方塊消失問題一起修正
- CSS / JS cache-bust 更新為 v0.10.2


## v0.11.0：迎賓頁 2.0｜完全重製

這版停止延續 v0.9.x～v0.10.x 的迎賓特效架構。

### 核心重做
- 「時盡」本體改為 Canvas 直接繪製
- 沙化粒子也在同一張 Canvas 中生成
- 粒子來源直接採樣該 Canvas 的中文字像素
- 因此文字與沙粒共用 100% 相同座標系，不再有錯位問題
- 不再用 CSS mask，不會再出現方塊 / 馬賽克式消失

### 沙化邏輯
- 採樣「時」「盡」實際像素
- 依字底→字頂排序
- 每個像素加入連續隨機擾動
- 使用圓形柔邊擦除，不使用矩形格
- 被侵蝕的位置同步生成極細沙粒
- 主要為冷白灰，只有極少量暗血紅
- hover 只掉極少量字底細砂作為互動暗示

### 進場流程
1. 「時盡」從黑暗中顯影
2. 輕觸「時盡」
3. 筆畫由下往上自然沙化
4. THE END OF TIME 同步從模糊殘影凝聚
5. 迎賓層淡出
6. 正式首頁開始

### 技術原則
本版之後若再調沙化，只調 Welcome 2.0 元件；
不再回頭疊加舊版迎賓 CSS / JS。


## v0.11.1：THE END OF TIME 凝聚收尾
- Welcome 2.0 沙化架構不動
- 英文最終清晰度提高到約 85%
- 保留極輕微 blur 與暗紅殘影
- 不變成普通實心白字


## v0.12.0：家式角色頁 1.0

新增正式角色頁：
- `characters/jiashi.html`
- `app/character.css`
- `app/jiashi.js`

公開頁內容：
- 家式大型主視覺
- 正式稱號 / 角色定位
- 世界法則
- 武器「斷刻」
- 基礎能力
- `家皇歿式・零`
- 零式主視覺
- 詩號
- 完整設定防雷入口預留

圖片仍從 Google Drive → GAS 圖片資料自動讀取：
- 家式常體主視覺
- 家式零式 / 殺體主視覺
- 斷刻展開
- 斷刻收攏

首頁家式角色卡現在可直接點進角色頁。
其餘三名角色卡目前維持展示，等待各自角色頁建立。


## v0.13.0：四角色頁完整建立

`characters/` 目前一次建立完成：
- `jiashi.html` — 時盡・家式
- `baiji.html` — 無晝・白霽
- `yeshenxing.html` — 月半刀・葉慎行
- `anyanxiu.html` — 蝶君・安言修

四張首頁角色卡全部可點擊進入對應角色頁。

角色頁共用：
- `app/character.css`
- `app/character-page.js`

圖片仍由 Google Drive → GAS 自動讀取主視覺。
家式額外讀取零式與斷刻展開／收攏圖。

注意：
- 安言修武器名稱仍為 TBD，因此公開頁不自行命名。
- 四角色頁目前皆為無雷公開版，完整設定入口保留待後續建置。


## v0.13.1：圖片自動縮放修正

- 武器圖片改為 `object-fit: contain`
- 斷刻展開 / 收攏會完整縮放在框內，不再硬裁切
- 零式主視覺也改為完整顯示
- 角色主視覺仍維持 `cover`，但改為上方優先對焦
- 桌機 / 手機各自保留適當內距


## v0.13.2：圖片完整顯示 + 返回首頁略過迎賓

圖片：
- 零式主視覺改為依圖片原始比例完整顯示
- 斷刻展開 / 收攏改為依圖片原始比例完整顯示
- 不再固定高度硬裁切

導覽：
- 從任何角色頁按「返回角色」或左上品牌返回首頁時
- 會自動略過 Welcome 2.0 迎賓頁
- 直接回到正式首頁 / 角色區
- 網址上的 `skipWelcome=1` 會自動清除，不會留在網址列
- 第一次直接開網站仍然正常顯示迎賓


## v0.13.3：角色技能介紹

四名角色頁新增技能 / 戰鬥能力區。

家式：
- 時字訣・止刻
- 空字訣・斷界移影
- 血字訣・血迴
- 弒字訣・絕脈
- 家皇歿式・零

白霽：
- 血憶
- 古血之軀（僅公開層級描述）

葉慎行：
- 基本功
- 血身控制

安言修：
- 黑蝶戰鬥識別
- 正道武學
- 武器與具名招式仍維持 TBD，不自行捏造

技能區採細線 / 字訣識別排版，不使用大量圓角卡片。


## v0.13.4：圖片原始比例自動偵測

這次不再用 CSS 猜高度。

- 圖片載入後直接讀 `naturalWidth / naturalHeight`
- 零式主視覺容器自動切換成圖片原始比例
- 斷刻展開 / 收攏各自依自己的原始比例顯示
- 修正 `.zero-section` 的 Grid stretch，避免容器被左側文字高度拉扁
- 圖片全部使用 `object-fit: contain`
- 因此不論原圖是方形、橫圖或直圖，都會完整顯示，不再裁頭、裁扇或裁邊


## v0.13.5：圖片配對 + 全站字級可讀性

### 圖片
角色主視覺不再綁死 `target` / `category` 完全相等。
現在會同時比對：
- 角色名
- 完整稱號
- 檔名
- 分類
- 路徑
- 資料夾資訊

支援別名：
- 家式 / 時盡・家式
- 白霽 / 無晝・白霽
- 葉慎行 / 月半刀・葉慎行
- 安言修 / 蝶君・安言修

Console 會顯示 `[圖片配對]` 或 `[圖片配對失敗]`，方便後續除錯。

### 字級
不再要求逐項回報。
整個網站做第一輪可讀性統一：
- 功能標籤最低約 11px
- 次要資訊 13px
- 正文 15px
- 角色頁 lead 18px
- 技能說明 13~14px
- 行距同步加大
- 手機版也維持閱讀尺寸


## v0.13.6：圖片裁切 + 可讀性整合修正

本版依確認清單一次整合：

### 角色主視覺
- 圖片載入後讀取原始寬高比
- 直圖 / 方圖：改用 `contain`，優先完整顯示角色
- 很寬的橫圖：才使用 `cover`
- 不再以塞滿容器為第一目標

### 零式 / 斷刻
- 容器直接跟隨原始圖片比例
- 一律 `contain`
- 不裁頭、不裁腳、不裁扇骨、不裁流蘇

### 四角色圖片配對
- 沿用 v0.13.5 寬鬆比對
- 支援角色名、完整稱號、檔名、路徑、分類等欄位

### 全站字級
- 角色頁與首頁再做第二輪可讀性放大
- 小標籤、meta、技能說明、返回連結、footer 全部統一提高
- 桌機 / 手機皆同步調整

### 返回首頁
- 角色頁返回首頁 / 角色區，直接略過 Welcome 2.0
- 第一次直接開網站仍正常顯示迎賓


## v0.14.0：網站文案資料化

本版開始正式採用：
試算表 → GAS → JS render → HTML

首頁：
- Welcome 英文 / 提示
- 品牌副標
- PUBLIC 模式文字
- Hero 文案
- 首頁按鈕
- 角色 / 圖庫 / 世界觀區塊標題與說明
皆優先讀取 `GAS.copy`

角色頁：
- 四頁改為同一套骨架
- 角色名稱、稱號、介紹、核心句、武器、戰鬥識別、狀態
- 公開詳述
- 招式 / 能力
- 關係
- 詩號
- 防雷區 UI 文案
全部改由 `GAS.characters / skills / relations / copy` 渲染

HTML 仍保留 SEO / 最低限度 fallback，
但日常可見內容的正式來源已改為試算表。

注意：
- 本版需要已完成 Data 1.0 migration
- 並使用已整合 `skills / relations / copy` 的永久 GAS CORE


## v0.14.1：武器資料正式接管

角色頁不再依賴 `char.weapon` / `weaponDescription` 等相容欄位。
正式改用：

`character.weaponId → GAS.weapons → weapon`

武器區顯示來源：
- 名稱 → `weapon.name`
- 公開介紹 → `weapon.publicDescription`
- 代表句 → `weapon.signatureLine`
- 戰鬥識別 → `weapon.combatSignature`

斷刻圖片仍由圖片 API 配對：
- 武器 / 斷刻 / 展開
- 武器 / 斷刻 / 收攏

若圖片找不到，對應空槽直接隱藏，不再留下空線。


## v0.14.2：斷刻圖片精準配對

修正武器圖片配對邏輯。

現在直接依：
`圖片.target = weapon.name`
再以檔名 / 路徑中的：
- 展開
- 收攏

分別配對。

例如目前圖片表：
- 斷刻_展開_主視覺_01.png
- 斷刻_收攏_主視覺_01.png

會分別進入展開與收攏圖片槽。

不再依賴模糊分數門檻。


## v0.14.3：mountImage 修復

修正 v0.14.2 中 `mountImage()` 被截斷的問題。

恢復：
- `img.loading = "eager"`
- 圖片自然尺寸讀取
- Hero cover / contain 判斷
- 零式與武器圖片自然比例
- `target.appendChild(img)`

本版不修改 GAS、不修改試算表、不修改 Drive。


## v0.14.4 DEBUG

暫時版，只用來查斷刻圖片不顯示的真正原因。

角色頁右下角會直接顯示：
- characterId / weaponId
- 找到的武器資料
- images 數量
- 與斷刻匹配的圖片列
- 展開 / 收攏實際挑到哪張
- weaponOpen / weaponClosed 是否存在、是否 hidden
- 是否 append `<img>`
- 圖片 load 成功尺寸
- 圖片 load error URL

查完後不要長期保留這版。


## v0.14.5：武器圖片 CSS 清理

本版依 v0.14.4 DEBUG 結果確認：
- 武器圖片資料已成功匹配
- `<img>` 已成功 append
- Drive 圖片已成功 load
- 容器有實際高度

因此本版只清理前端 CSS。

處理：
- 移除舊版重複 / 衝突的 `.weapon-slot` 規則
- 建立唯一武器圖片顯示來源
- 圖片固定 `object-fit: contain`
- 圖片固定 `z-index: 1`
- 展開 / 收攏標籤固定 `z-index: 2`
- 保留自然比例
- 手機版單欄
- 移除 DEBUG 面板

不修改：
- GAS
- 試算表
- Drive
- 圖片配對邏輯


## v0.14.6：圖片對象ID正式勾稽

武器圖片顯示不再判斷角色 ID、武器名稱或特定武器狀態。

正式資料流：
`character.weaponId → weapons.id → images.targetId`

前端規則：
- 0 張圖片：隱藏圖片區
- 1 張圖片：單張
- 2 張圖片：雙欄
- 3 張以上：自動 grid
- 圖片標籤優先從檔名第二段自動取得
  - 斷刻_展開_主視覺_01.png → 展開
  - 墨蝶_正面_主視覺_01.png → 正面

前端不再包含：
- `id === "jiashi"`
- `weapon.id === "duanke"`
- 任何特定角色 / 武器的顯示條件


## v0.14.7：角色介紹三欄通用化

角色 Hero 下方三格不再固定為：
- 戰鬥識別
- 狀態
- 立場

改為完全資料驅動：

角色表：
- 角色介紹欄1標題
- 角色介紹欄1內容
- 角色介紹欄2標題
- 角色介紹欄2內容
- 角色介紹欄3標題
- 角色介紹欄3內容

前端只負責依序顯示 1 / 2 / 3。
每個角色都可以使用完全不同的三組標題與內容，
不需要修改 JS。


## v0.14.8：角色資料去重

角色 Hero 三格正式只讀：
- 角色介紹欄1標題 / 內容
- 角色介紹欄2標題 / 內容
- 角色介紹欄3標題 / 內容

前端不再讀：
- char.signature
- char.state

角色摘要列也不再重複顯示舊戰鬥識別 / 狀態，
保留 IDENTITY / CORE。


## v0.14.9：首頁封面與角色印記特效

### 1) 首頁封面正式改為資料驅動
首頁 Hero 圖優先從 `圖片` 資料中抓：
- `大分類 = 網站素材`
- 檔名包含 `首頁`
- 並包含 `封面` 或 `主視覺`

若找不到，才退回舊的家式主視覺。

建議首頁封面命名：
- `首頁_封面_主視覺_01.png`

### 2) 角色卡印記 hover 特效加強
首頁角色卡在滑鼠移過去時：
- 家式 `家`：血氣喚醒、血痕下滲
- 白霽 `白`：一抹笑弧浮現
- 葉慎行 `月半`：保留刀痕
- 安言修 `蝶`：黑蝶飛舞更明顯


## v0.15.0：小說閱讀架構 1.0

網站定位從「角色設定站」正式調整為：
**原創小說作品站 + 世界觀資料庫**

首頁順序：
1. 故事
2. 角色
3. 世界觀

已移除：
- 角色圖庫區塊
- 首頁角色圖庫按鈕

新增：
- `story/index.html`：章節目錄
- `story/read.html?id=...`：正文閱讀頁
- `app/story.js`
- `app/story.css`
- 首頁故事預覽
- 未公開章節鎖定狀態
- 上一節 / 章節目錄 / 下一節
- 手機閱讀版字級、行距、段寬

故事資料完全由試算表 `劇情` → GAS → 前端讀取。

### 劇情表 v1.5
- ID
- 章序
- 章名
- 節序
- 節名
- 副標
- 公開摘要
- 正文
- 排序
- 是否公開
- 是否開放閱讀
- 防雷內容
- 作者備註

### 網址
目前 GitHub Pages 採穩定的：
`story/read.html?id=ch01-01`

資料 API 同時回傳 `cleanRoute = story/ch01-01.html`，
未來切到支援 rewrite / build 的部署方式時，可以直接升級成乾淨網址，
不用改試算表資料。


## v0.15.1：內頁導覽列重製

原本內頁左上角：
- 小「時」方框
- WORLD / CHARACTER ARCHIVE
- 返回角色
- PUBLIC・無雷

已整體淘汰。

新導覽列固定三區：
1. 左：正式 Logo + 時盡
2. 中：breadcrumb / 主要導覽
3. 右：公開・無雷狀態

角色頁：
`首頁 / 角色 / 角色名`

故事目錄：
`首頁 / 章節目錄`

故事閱讀頁：
`首頁 / 故事 / 第一章 第一節`

首頁：
`Logo + 時盡 | 故事 / 角色 / 世界觀 | 公開・無雷`

Logo 正式路徑：
`assets/images/the-end-of-time-logo.png`

本版不修改 GAS CORE。
新增的導覽文字放在「網站文案」，請執行一次：
`migrateNavigationCopyV1_6()`


## v0.15.2：Logo 可讀性 + Favicon

### Logo
正式使用：
`assets/images/the-end-of-time-logo.png`

因 Logo 本體以黑色為主，本版不修改圖片，而是在網站端增加：
- 極淡白色輪廓
- 紅紫冷光
- 很弱的圓形背景光暈
- hover 時輕微增亮

### Favicon
正式使用：
`assets/images/favicon.png`

已接入：
- 首頁
- 四個角色頁
- 故事目錄頁
- 故事閱讀頁

同時加入 `apple-touch-icon`。


## v0.15.3：大 Logo 導覽列

Logo 改成完整橫向徽記尺寸：
- 桌面：約 160px
- 中型螢幕：約 130px
- 平板：約 112px
- 手機：約 92px

Header 高度同步增加，避免 Logo 被壓縮。
移除上一版圓形背景光暈，只保留很淡的輪廓陰影。


## v0.16.0：作品站導覽 1.0

### 首頁
首頁不再放完整角色 / 世界觀內容。
只保留：
- Hero
- 故事入口
- 角色入口
- 世界觀入口

### 獨立頁面
- `/story/`：故事目錄
- `/characters/`：角色總覽
- `/world/`：世界觀總覽

### Topbar
正式改為冷白底：
- 左：Logo + 時盡
- 中：故事 / 角色 / 世界觀
- 右：公開・無雷 / 完整・有雷

### 有雷模式
點擊「公開・無雷」不會直接開啟完整資料。
一定先顯示二次確認視窗：

「開啟完整設定？」
「將顯示人物死亡、完整身世、世界核心真相與劇情內容。」

按下「開啟有雷內容」後才切到 `type=full`。
模式保存在瀏覽器 localStorage。

完整模式點一下可直接切回無雷模式。

### 資料來源
有雷 / 無雷都使用原本 GAS：
- `?type=public`
- `?type=full`

不需要更改永久 CORE。


## v0.16.1：全站捲軸視覺統一

全站瀏覽器捲軸改為《時盡》視覺：
- 軌道：近黑 / 深灰
- 滑塊：冷灰黑
- hover：血紅 → 紫色分界
- active：紅紫對比稍加強
- 寬度：10px
- 低圓角，避免 SaaS 感
- Firefox / Chromium / Edge / Safari 相容樣式
- 手機橫向導覽列仍隱藏自身捲軸


## v0.16.2：整合修正

本版一次處理：

### Topbar
- 導覽字級放大
- 模式標籤放大
- Topbar 強制置頂最高層
- 角色圖片不再蓋過 Topbar

### 模式名稱
- `無雷模式`
- `爆雷模式`
- 開啟爆雷模式仍必須二次確認

爆雷模式開啟後，角色頁底部「完整設定 / 防雷模式」區塊直接移除，
因為此時已經是完整資料狀態。

### 角色頁印記 hover
- 家：吸血 / 血滴
- 白：冷淡笑弧
- 月半：刀痕
- 蝶：黑蝶飛舞

### 捲軸
不再依賴瀏覽器原生捲軸樣式。
改成網站自己的右側拉桿：
- 平常冷灰
- hover：血紅 → 紫
- 可以拖曳
- 可以點擊軌道跳轉
- **從 Topbar 下方開始，不會延伸進白色 Topbar**
- 手機不顯示自訂拉桿

### Loading
所有需要從 GAS / 試算表抓資料的頁面加入讀取畫面：
`時盡 / 讀取時序資料中……`

資料讀取完成或失敗後都會自動淡出。


## v0.16.3：Loading 動圖 + 介面整合修正

### Loading
正式改用四層 PNG：
- `assets/images/loading/hourglass-frame.png`
- `assets/images/loading/sand-top.png`
- `assets/images/loading/sand-bottom.png`
- `assets/images/loading/sand-stream.png`

動畫：
- 整顆沙漏翻轉
- 上砂逐漸減少
- 中央流砂
- 下砂逐漸增加
- 2.6 秒循環

Loading 結束機制：
- GAS 成功：關閉
- GAS 失敗 / fallback：關閉
- 初始化意外失敗：finally 關閉
- 最後保險：12 秒 fail-safe 自動關閉
因此不再允許 Loading 永久卡住。

### 首頁標題
`時盡｜THE END OF TIME`

### Topbar
- 導覽字放大
- 模式標籤放大
- 圖片不再蓋過 Topbar

### 模式
- 無雷模式
- 爆雷模式
- 爆雷模式仍需二次確認
- 爆雷模式下角色頁不再顯示「完整設定 / 防雷區」

### 角色頁印記
- 家：血色 / 吸血感
- 白：冷笑弧
- 月半：刀痕
- 蝶：黑蝶飛舞

### 捲軸
自訂拉桿只從白色 Topbar 下方開始。
hover 明顯使用「血紅 → 紫」漸層。


## v0.16.4：首頁主視覺 + 角色印記特效

### 首頁主視覺
- 來源仍為 Drive → `網站素材`
- 前端不寫死 URL
- 優先抓「首頁 / 封面 / 主視覺」
- 支援 hero / cover
- 若 `網站素材` 只有一張一般圖片，也可自動採用
- 最後才 fallback 到家式常體主視覺
- Logo / favicon / OG / loading 不會誤抓

顯示方式：
- 移除硬框
- 移除 IMAGE SLOT
- 移除卡片底色
- 四周羽化
- 自然融入首頁黑色背景

### Logo
Topbar 高度不增加。
Logo 在既有高度內放大到接近滿版。

### 角色印記
首頁已無角色卡，因此只維護：
- `/characters/` 角色總覽
- 四個角色詳細頁

改成 JS 明確觸發，每次滑入都可重播：
- 家：吸血 / 血滴
- 白：冷淡笑弧
- 月半：刀痕
- 蝶：黑蝶飛舞

本版不需要修改 GAS，也不需要新的 Migration。
